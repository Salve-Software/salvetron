///
/// HybridNitroMakoSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `NativeLogEntry` to properly resolve imports.
namespace margelo::nitro::mako { struct NativeLogEntry; }
// Forward declaration of `DeviceInfoResult` to properly resolve imports.
namespace margelo::nitro::mako { struct DeviceInfoResult; }

#include "NativeLogEntry.hpp"
#include <functional>
#include "DeviceInfoResult.hpp"
#include <string>
#include <optional>

namespace margelo::nitro::mako {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `NitroMako`
   * Inherit this class to create instances of `HybridNitroMakoSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridNitroMako: public HybridNitroMakoSpec {
   * public:
   *   HybridNitroMako(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridNitroMakoSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridNitroMakoSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridNitroMakoSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual double sum(double num1, double num2) = 0;
      virtual bool startLogCapture(const std::function<void(const NativeLogEntry& /* log */)>& onLog) = 0;
      virtual void stopLogCapture() = 0;
      virtual bool isCapturing() = 0;
      virtual DeviceInfoResult getDeviceInfo() = 0;
      virtual std::optional<std::string> getStoredDeviceId() = 0;
      virtual void storeDeviceId(const std::string& deviceId) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "NitroMako";
  };

} // namespace margelo::nitro::mako
